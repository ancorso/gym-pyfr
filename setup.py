from setuptools import setup

setup(name='gym_pyfr',
      version='0.0.1',
      install_requires = [
        'gym',
        'appdirs >= 1.4.0',
        'gimmik >= 2.0',
        'h5py >= 2.6',
        'mako >= 1.0.0',
        'mpi4py >= 2.0',
        'numpy >= 1.8',
        'pytools >= 2016.2.1'
    ]
)